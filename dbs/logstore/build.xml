<project default="createdb">

  <property file="../../www/autochef/etc/logstore.properties" />

  <path id="compile.classpath">
    <fileset dir="../../www/autochef/src/lib" />
  </path>

  <target name="clean">
    <sql
        driver="org.postgresql.Driver"
        classpathref="compile.classpath"
        url="jdbc:postgresql://${database.hostname}/postgres"
        userid="${database.username}"
        password="${database.password}"
        autocommit="true"
    >
    drop database if exists logstore;
    </sql>
  </target>

  <target name="createdb" depends="clean">
    <sql
        driver="org.postgresql.Driver"
        classpathref="compile.classpath"
        url="jdbc:postgresql://${database.hostname}/postgres"
        userid="${database.username}"
        password="${database.password}"
        autocommit="true"
    >
    create database logstore;
    </sql>
    <sql
        driver="org.postgresql.Driver"
        classpathref="compile.classpath"
        url="jdbc:postgresql://${database.hostname}/${database.catalog}"
        userid="${database.username}"
        password="${database.password}"
        src="scripts/create.sql"
    />
    <sql
        driver="org.postgresql.Driver"
        classpathref="compile.classpath"
        url="jdbc:postgresql://${database.hostname}/${database.catalog}"
        userid="${database.username}"
        password="${database.password}"
        delimiter="$"
    >
      <path>
        <fileset dir="functions">
          <include name="**/*.sql" />
        </fileset>
      </path>
    </sql>
  </target>

</project>
